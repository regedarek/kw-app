# ⚠️  WARNING: This file is committed to git!
# Only use Bitwarden fetch commands: $(bw get item ...)
# NEVER put actual secret values in this file!

# Shared secrets across all environments
# Fetched dynamically from Bitwarden OR from environment variables (CI/CD)

# Docker Registry credentials (shared between staging and production)
# In CI/CD: Use GitHub Actions secrets KAMAL_REGISTRY_USERNAME and KAMAL_REGISTRY_PASSWORD
# Locally: Fetch from Bitwarden
KAMAL_REGISTRY_USERNAME=${KAMAL_REGISTRY_USERNAME:-$(bw get item kw-app-staging-docker-registry --session $BW_SESSION 2>/dev/null | jq -r '.login.username')}
KAMAL_REGISTRY_PASSWORD=${KAMAL_REGISTRY_PASSWORD:-$(bw get item kw-app-staging-docker-registry --session $BW_SESSION 2>/dev/null | jq -r '.login.password')}