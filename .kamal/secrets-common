# Kamal secrets - common (shared across environments)
# 
# This file reads from environment variables by default (for CI/CD).
# - In CI/CD: GitHub Actions sets these as environment variables
# - Locally: Run .kamal/bw-fetch-secrets.sh to create .kamal/secrets-common.local with actual values
#
# Do not hardcode secrets here - this file is committed to git!

# If .local file exists (generated by bw-fetch-secrets.sh), source it
# Otherwise, read from environment variables
if [ -f "$(dirname "$0")/secrets-common.local" ]; then
    source "$(dirname "$0")/secrets-common.local"
else
    # Docker Registry credentials (from environment variables)
    KAMAL_REGISTRY_USERNAME=${KAMAL_REGISTRY_USERNAME}
    KAMAL_REGISTRY_PASSWORD=${KAMAL_REGISTRY_PASSWORD}
fi