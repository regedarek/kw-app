---
# Bitwarden Secrets Manager UUIDs
# These are the UUIDs of secrets stored in Bitwarden Secrets Manager
# Get these values by running: bws secret list --access-token "YOUR_TOKEN"

# Infrastructure secrets (UUIDs from Bitwarden Secrets Manager)
bws_vps_sudo_uuid: "45e88671-20f8-4c23-ae00-b3da011bafdc"  # kw-app-production-vps-sudo-password
bws_registry_username_uuid: "6e77be63-a052-4594-9585-b3da01100ce9"  # registry-username (shared)
bws_registry_password_uuid: "6ff8b019-a3e4-4571-85fc-b3da01106838"  # registry-password (shared)

# Server configuration (non-secret values)
production_host: "146.59.44.70"
production_hostname: "nowypanel.kw.krakow.pl"
production_user: "ubuntu"

# Container versions
postgres_version: "16.0"
redis_version: "7-alpine"

# Database configuration
database_name: "kw_app_production"
database_user: "production_user"

# Docker network
docker_network: "kw-app-network"