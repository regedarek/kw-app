# ⚠️ DEPRECATED

Ansible provisioning replaced by GitHub Actions.

**Old:** Manual Ansible + `bw` CLI + manual secret export
**New:** Automatic deployment + `bws` CLI + auto secret fetch

Migration: https://github.com/rockcodelabs/infrastructure/blob/main/docs/BITWARDEN-MIGRATION.md