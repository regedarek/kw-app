table.stack
  thead
    tr
      th
        | Rok
      th
        | Kwota
      th
        | Opłacono
  tbody
    - fees.each do |fee|
      tr
        td
          = fee.year
        td
          = fee.cost
        td
          - if fee.order && fee.order.payment
            = fee.order.payment.prepaid? || fee.order.payment.cash? ? "Zapłacone" : "Nie zapłacone"
