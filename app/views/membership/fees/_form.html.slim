= form_for form, url: membership_fees_path do |f|
  - if form.errors.any?
    .callout.alert
      ul
        - form.errors.full_messages.each do |msg|
          li= msg
  .input-group
    span.input-group-label
        - if current_user.profile.youth? || current_user.profile.position.include?("retired")
          | 100 zł
        - else
          | 150 zł
    = f.number_field :year, min: 2023, max: 2024, class: 'input-group-field', value: 2023
    .input-group-button
      = f.submit 'Zapłać', class: 'button'
  - if Management::SettingsRecord.find_by(path: 'plastic')&.content == 'Tak'
    .input-group
      = f.label :plastic, 'Plastikowa legitymacja (+15zł)'
      = f.check_box :plastic
