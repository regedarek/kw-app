pl:
  limit_exceded: 'Niestety nie ma już miejsc!'
  not_last_fee: 'nie została opłacona!'
  email_not_unique: 'Ten e-mail jest już zapisany!'
  closed_or_open: 'Zapisy zamknięte'
  slug?: 'jest niepoprawny, użyj znaków a-z oraz -'

  errors:
    package_required: 'musi zostać wybrany!'
    filled?: "musi być wypełnione"
    rules:
      name:
        filled?: 'Nazwa przejścia musi być podana'
      climbing_date:
        filled?: 'Data przejścia musi być wypełniona'
      rating:
        filled?: 'Ocena przejścia musi zostać wybrana'
      place:
        filled?: 'Miejsce wydarzenia nie może być puste'
      payment_type:
        filled?: 'Konto płatności musi zostać wybrane'
      organizator_id:
        filled?: 'Koordynator musi zostać wskazany'
      slug:
        unique?: 'Adres strony już istnieje'
        max_size?: 'Adres strony musi mieć mniej niż 31 znaków'
        slug?: 'Adres strony jest niepoprawny, użyj znaków a-z oraz -'
        filled?: 'Adres strony wydarzenia nie może być puste'
en:
  errors:
    rules:
      team_name:
        filled?: Team name has to be filled
    unique?: 'already exists'
    slug?: 'Adres wydarzenia jest niepoprawny'
    or: "or"
    array?: "must be an array"
    empty?: "must be empty"
    excludes?: "must not include %{value}"
    excluded_from?:
      arg:
        default: "must not be one of: %{list}"
        range: "must not be one of: %{list_left} - %{list_right}"
    exclusion?: "must not be one of: %{list}"
    eql?: "must be equal to %{left}"
    not_eql?: "must not be equal to %{left}"
    filled?: "must be filled"
    format?: "is in invalid format"
    number?: "must be a number"
    odd?: "must be odd"
    even?: "must be even"
    gt?: "musi być większe niż %{num}"
    gteq?: "must be greater than or equal to %{num}"
    hash?: "must be a hash"
    included_in?:
      arg:
        default: "must be one of: %{list}"
        range: "must be one of: %{list_left} - %{list_right}"
    inclusion?: "must be one of: %{list}"
    includes?: "must include %{value}"
    bool?: "must be boolean"
    true?: "musi być zaznaczone"
    false?: "must be false"
    int?: "must be an integer"
    float?: "must be a float"
    decimal?: "must be a decimal"
    date?: "must be a date"
    date_time?: "must be a date time"
    time?: "must be a time"
    key?: "is missing"
    attr?: "is missing"
    lt?: "must be less than %{num}"
    lteq?: "must be less than or equal to %{num}"
    max_size?: "size cannot be greater than %{num}"
    min_size?: "size cannot be less than %{num}"
    none?: "cannot be defined"
    str?: "must be a string"
    type?: "must be %{type}"
    size?:
      arg:
        default: "size must be %{size}"
        range: "size must be within %{size_left} - %{size_right}"
      value:
        string:
          arg:
            default: "length must be %{size}"
            range: "length must be within %{size_left} - %{size_right}"
