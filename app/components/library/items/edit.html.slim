= form_for @item, as: :item, url: ksiazka_path(@item.id), method: :put do |f|
  = render 'form', item: @item, f: f

  .row
    .large-12.columns
      = f.submit 'Edytuj egzemplarz', class: 'button'
      = link_to 'wróć do biblioteki', biblioteka_path, class: 'button'
